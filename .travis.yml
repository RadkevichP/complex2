sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pradkevich/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true pradkevich/react-test npm test

after_success:
  - docker build -t pradkevich/multi-client ./client
  - docker build -t pradkevich/multi-nginx ./nginx
  - docker build -t pradkevich/multi-server ./server
  - docker build -t pradkevich/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push pradkevich/multi-client
  - docker push pradkevich/multi-nginx
  - docker push pradkevich/multi-server
  - docker push pradkevich/multi-worker
